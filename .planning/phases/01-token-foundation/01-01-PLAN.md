---
phase: 01-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - tests/tokens/color.test.ts
  - tests/tokens/typography.test.ts
  - tests/tokens/layout.test.ts
  - tests/tokens/iconography.test.ts
  - tokens/manifest/extraction-map.json
autonomous: true
requirements:
  - TOKN-01
  - TOKN-02
  - TOKN-03
  - TOKN-04
must_haves:
  truths:
    - "The repository has a repeatable token build and test entrypoint instead of ad hoc token files."
    - "The HTML source is audited into an explicit extraction contract for color, typography, layout, and iconography."
    - "Automated token tests exist for every Phase 1 requirement before implementation begins."
  artifacts:
    - path: "package.json"
      provides: "Token build and validation scripts plus dependency manifest"
    - path: "tokens/manifest/extraction-map.json"
      provides: "HTML-to-token extraction contract with source references"
    - path: "tests/tokens/color.test.ts"
      provides: "Color token verification scaffold"
    - path: "tests/tokens/typography.test.ts"
      provides: "Typography token verification scaffold"
    - path: "tests/tokens/layout.test.ts"
      provides: "Spacing/elevation/radius/grid verification scaffold"
    - path: "tests/tokens/iconography.test.ts"
      provides: "Iconography verification scaffold"
  key_links:
    - from: "package.json"
      to: "tokens/style-dictionary.config.ts"
      via: "tokens:build script"
      pattern: "tokens:build"
    - from: "tests/tokens/color.test.ts"
      to: "tokens/manifest/extraction-map.json"
      via: "source audit assertions"
      pattern: "extraction-map"
    - from: "tests/tokens/iconography.test.ts"
      to: "8.costra-design-system.html"
      via: "documented icon rules and inventory expectations"
      pattern: "stroke-width|24px|icon"
---

<objective>
Create the token workspace, extraction contract, and automated test scaffolding for Phase 1.

Purpose: Give the executor a concrete Wave 0 foundation so the token implementation is driven by the audited HTML source instead of improvised file structure.
Output: Package/test config, extraction-map artifact, and requirement-mapped token test files.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Design system with codex/.codex/get-shit-done/workflows/execute-plan.md
@D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Design system with codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-token-foundation/01-CONTEXT.md
@.planning/phases/01-token-foundation/01-RESEARCH.md
@.planning/phases/01-token-foundation/01-VALIDATION.md
@8.costra-design-system.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the token workspace and validation entrypoints</name>
  <read_first>AGENTS.md, CLAUDE.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/01-token-foundation/01-CONTEXT.md, .planning/phases/01-token-foundation/01-RESEARCH.md, .planning/phases/01-token-foundation/01-VALIDATION.md, package.json (if present), tsconfig.json (if present), vitest.config.ts (if present)</read_first>
  <files>package.json, tsconfig.json, vitest.config.ts</files>
  <action>Create a Node workspace for the design-token pipeline. Add `package.json` with the exact scripts `"tokens:build": "style-dictionary build --config tokens/style-dictionary.config.ts"`, `"test": "vitest run"`, and `"test:tokens": "vitest run tests/tokens"`. Add devDependencies for `style-dictionary`, `typescript`, and `vitest`, plus the minimum package metadata needed for a private workspace. Add `tsconfig.json` configured for `module: "NodeNext"`, `moduleResolution: "NodeNext"`, `target: "ES2022"`, `resolveJsonModule: true`, `esModuleInterop: true`, and includes for `tokens`, `src`, and `tests`. Add `vitest.config.ts` that includes only `tests/tokens/**/*.test.ts`, runs in the `node` environment, and keeps execution single-run with no watch behavior. This task establishes the exact script and config surface that later plans must implement against.</action>
  <acceptance_criteria>`package.json` contains the exact strings `"tokens:build": "style-dictionary build --config tokens/style-dictionary.config.ts"` and `"test:tokens": "vitest run tests/tokens"`; `tsconfig.json` contains `"module": "NodeNext"` and `"resolveJsonModule": true`; `vitest.config.ts` contains `include: ['tests/tokens/**/*.test.ts']` and `environment: 'node'`.</acceptance_criteria>
  <verify>
    <automated>npm install</automated>
  </verify>
  <done>The repo can install the token toolchain and exposes stable build/test commands for later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the HTML source into an extraction map and requirement test scaffolds</name>
  <read_first>8.costra-design-system.html, .planning/REQUIREMENTS.md, .planning/phases/01-token-foundation/01-CONTEXT.md, .planning/phases/01-token-foundation/01-RESEARCH.md, tokens/manifest/extraction-map.json (if present), tests/tokens/color.test.ts (if present), tests/tokens/typography.test.ts (if present), tests/tokens/layout.test.ts (if present), tests/tokens/iconography.test.ts (if present)</read_first>
  <files>tokens/manifest/extraction-map.json, tests/tokens/color.test.ts, tests/tokens/typography.test.ts, tests/tokens/layout.test.ts, tests/tokens/iconography.test.ts</files>
  <action>Create `tokens/manifest/extraction-map.json` as the canonical audit file for Phase 1. It must enumerate source categories for `color`, `typography`, `spacing`, `elevation`, `radius`, `grid`, and `iconography`, and store exact HTML references such as `--c-accent`, `--shadow-glow`, `--grid-columns`, `.type-sample-d1`, `.type-sample-code`, and the icon SVG rules at the iconography section. Then create the four Vitest files under `tests/tokens/` as scaffolded but concrete requirement contracts: `color.test.ts` asserts the future token build must expose foundation and semantic color groups with source metadata; `typography.test.ts` asserts font families plus role tokens for `display-1`, `display-2`, `heading-1`, `heading-2`, `heading-3`, `body`, `caption`, and `code`; `layout.test.ts` asserts spacing, elevation, radius, and grid/breakpoint outputs including `40rem`, `48rem`, `64rem`, `80rem`, and `96rem`; `iconography.test.ts` asserts canonical icon sizes, `strokeWidth: 1.5`, round linecap/linejoin, and an initial icon inventory contract. Use `describe.todo` or `test.todo` only where a generated artifact does not exist yet, but keep the exact token names and expected values in the file so Plan 02 can activate them directly.</action>
  <acceptance_criteria>`tokens/manifest/extraction-map.json` contains the exact keys `"color"`, `"typography"`, `"spacing"`, `"elevation"`, `"radius"`, `"grid"`, and `"iconography"`; `tokens/manifest/extraction-map.json` contains the exact strings `"--c-accent"`, `".type-sample-d1"`, `"--grid-columns"`, and `"stroke-width"`; `tests/tokens/color.test.ts` contains `describe('color tokens'`; `tests/tokens/typography.test.ts` contains `display-1`; `tests/tokens/layout.test.ts` contains `40rem`; `tests/tokens/iconography.test.ts` contains `strokeWidth` and `1.5`.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/tokens --passWithNoTests</automated>
  </verify>
  <done>The repository has a source-backed extraction contract and one test scaffold per Phase 1 requirement.</done>
</task>

</tasks>

<verification>
Run `npm install` once after Task 1, then run `npx vitest run tests/tokens --passWithNoTests` after Task 2 to prove the workspace and scaffold files are valid.
</verification>

<success_criteria>
Phase 1 now has a concrete workspace skeleton, a source-backed extraction map, and requirement-specific tests ready to be activated by the token implementation plan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-foundation/01-01-SUMMARY.md`
</output>
