---
phase: 01-token-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tokens/foundation/color.tokens.json
  - tokens/foundation/typography.tokens.json
  - tokens/foundation/spacing.tokens.json
  - tokens/foundation/elevation.tokens.json
  - tokens/foundation/radius.tokens.json
  - tokens/foundation/grid.tokens.json
  - tokens/foundation/iconography.tokens.json
  - tokens/semantic/color.tokens.json
  - tokens/semantic/surface.tokens.json
  - tokens/style-dictionary.config.ts
  - src/tokens/generated/theme.css
  - src/tokens/generated/tokens.ts
  - src/tokens/generated/tokens.json
  - src/tokens/index.ts
  - tests/tokens/color.test.ts
  - tests/tokens/typography.test.ts
  - tests/tokens/layout.test.ts
  - tests/tokens/iconography.test.ts
autonomous: true
requirements:
  - TOKN-01
  - TOKN-02
  - TOKN-03
  - TOKN-04
must_haves:
  truths:
    - "Shared token definitions exist for color, typography, spacing, elevation, radius, grid, and iconography foundations."
    - "A small semantic layer sits on top of the raw foundation values without introducing component tokens."
    - "Future consumers can import generated CSS, JSON, and TypeScript tokens from one source instead of re-declaring constants."
  artifacts:
    - path: "tokens/foundation/color.tokens.json"
      provides: "Foundation color tokens with traceable metadata"
    - path: "tokens/foundation/typography.tokens.json"
      provides: "Typography primitives and role tokens"
    - path: "tokens/foundation/grid.tokens.json"
      provides: "Grid and breakpoint foundations"
    - path: "tokens/foundation/iconography.tokens.json"
      provides: "Iconography foundation metadata and inventory"
    - path: "src/tokens/generated/theme.css"
      provides: "Tailwind v4 theme variable output"
    - path: "src/tokens/generated/tokens.ts"
      provides: "Typed token export surface"
    - path: "src/tokens/index.ts"
      provides: "Stable token import entrypoint"
  key_links:
    - from: "tokens/semantic/color.tokens.json"
      to: "tokens/foundation/color.tokens.json"
      via: "alias references"
      pattern: "{foundation.color"
    - from: "tokens/style-dictionary.config.ts"
      to: "src/tokens/generated/theme.css"
      via: "style-dictionary build target"
      pattern: "theme.css"
    - from: "src/tokens/index.ts"
      to: "src/tokens/generated/tokens.ts"
      via: "stable re-export"
      pattern: "generated/tokens"
---

<objective>
Implement the Phase 1 token source, semantic alias layer, and generated consumer outputs.

Purpose: Turn the audited HTML foundations into a single token source that later Tailwind, React, and Storybook work can consume directly.
Output: Foundation token files, semantic alias files, Style Dictionary build config, generated CSS/JSON/TS artifacts, and activated requirement tests.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Design system with codex/.codex/get-shit-done/workflows/execute-plan.md
@D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Design system with codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-token-foundation/01-CONTEXT.md
@.planning/phases/01-token-foundation/01-RESEARCH.md
@.planning/phases/01-token-foundation/01-VALIDATION.md
@.planning/phases/01-token-foundation/01-01-SUMMARY.md
@8.costra-design-system.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the audited foundation token source and minimal semantic aliases</name>
  <read_first>.planning/phases/01-token-foundation/01-01-SUMMARY.md, .planning/phases/01-token-foundation/01-CONTEXT.md, .planning/phases/01-token-foundation/01-RESEARCH.md, 8.costra-design-system.html, tokens/manifest/extraction-map.json, tokens/foundation/color.tokens.json (if present), tokens/foundation/typography.tokens.json (if present), tokens/foundation/spacing.tokens.json (if present), tokens/foundation/elevation.tokens.json (if present), tokens/foundation/radius.tokens.json (if present), tokens/foundation/grid.tokens.json (if present), tokens/foundation/iconography.tokens.json (if present), tokens/semantic/color.tokens.json (if present), tokens/semantic/surface.tokens.json (if present), tests/tokens/color.test.ts, tests/tokens/typography.test.ts, tests/tokens/layout.test.ts, tests/tokens/iconography.test.ts</read_first>
  <files>tokens/foundation/color.tokens.json, tokens/foundation/typography.tokens.json, tokens/foundation/spacing.tokens.json, tokens/foundation/elevation.tokens.json, tokens/foundation/radius.tokens.json, tokens/foundation/grid.tokens.json, tokens/foundation/iconography.tokens.json, tokens/semantic/color.tokens.json, tokens/semantic/surface.tokens.json, tests/tokens/color.test.ts, tests/tokens/typography.test.ts, tests/tokens/layout.test.ts, tests/tokens/iconography.test.ts</files>
  <behavior>
    - Test 1: Color foundations expose renamed public tokens with `$extensions.costra.sourceCssVar` metadata and semantic aliases for cross-cutting usage only.
    - Test 2: Typography exposes font-family primitives plus role tokens for `display-1`, `display-2`, `heading-1`, `heading-2`, `heading-3`, `body`, `caption`, and `code`.
    - Test 3: Layout exposes spacing, elevation, radius, and grid tokens including breakpoint values expressed in `rem`.
    - Test 4: Iconography exposes canonical sizes, stroke rules, and an initial icon inventory without introducing React component tokens.
  </behavior>
  <action>Populate the foundation token files using DTCG-style objects and preserve HTML traceability per D-01 through D-04. In `tokens/foundation/color.tokens.json`, rename the public color groups into clear design-system names while keeping original CSS variables in `$extensions.costra.sourceCssVar` and section names in `$extensions.costra.sourceSection`; include raw accent-glow values and the status colors from the HTML. In `tokens/foundation/typography.tokens.json`, define font-family primitives for display/body/mono and role tokens for the eight named text styles extracted from the `.type-sample-*` rules. In `tokens/foundation/spacing.tokens.json`, keep the 4px-based scale from `0` through `24`; in `elevation.tokens.json`, keep `xs`, `sm`, `md`, `lg`, `xl`, and `glow` as distinct tokens; in `radius.tokens.json`, keep `0`, `xs`, `sm`, `md`, `lg`, `xl`, and `full`; in `grid.tokens.json`, store `columns: 12`, `gutter: 24px`, `margin: 48px`, `max: 1280px`, plus responsive breakpoint values converted to `40rem`, `48rem`, `64rem`, `80rem`, and `96rem`. In `tokens/foundation/iconography.tokens.json`, capture `canvas: 24`, `strokeWidth: 1.5`, `strokeLinecap: round`, `strokeLinejoin: round`, supported sizes, and the initial icon inventory from the HTML. Add only a small semantic layer in `tokens/semantic/color.tokens.json` and `tokens/semantic/surface.tokens.json` for cross-cutting aliases such as `text.primary`, `text.secondary`, `surface.canvas`, `surface.panel`, `border.default`, and `interactive.primary`; do not add button, card, input, badge, or other component tokens. Activate the four tests so they assert these exact token groups and metadata rather than using `todo` placeholders.</action>
  <acceptance_criteria>`tokens/foundation/color.tokens.json` contains `$extensions`, `sourceCssVar`, and `--c-accent`; `tokens/foundation/typography.tokens.json` contains the exact strings `display-1`, `heading-1`, and `code`; `tokens/foundation/grid.tokens.json` contains `40rem`, `48rem`, `64rem`, `80rem`, and `96rem`; `tokens/foundation/iconography.tokens.json` contains `"strokeWidth"` with value `1.5`; `tokens/semantic/color.tokens.json` contains `text.primary` and `interactive.primary`; none of the token source files contain the strings `"button"`, `"card"`, or `"input"` as token group names.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/tokens/color.test.ts tests/tokens/typography.test.ts tests/tokens/layout.test.ts tests/tokens/iconography.test.ts</automated>
  </verify>
  <done>The Phase 1 token source exists, is source-traceable, and the requirement tests for all token categories are active and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate the shared CSS, JSON, and TypeScript token outputs from one source</name>
  <read_first>package.json, tsconfig.json, tokens/style-dictionary.config.ts (if present), tokens/foundation/color.tokens.json, tokens/foundation/typography.tokens.json, tokens/foundation/spacing.tokens.json, tokens/foundation/elevation.tokens.json, tokens/foundation/radius.tokens.json, tokens/foundation/grid.tokens.json, tokens/foundation/iconography.tokens.json, tokens/semantic/color.tokens.json, tokens/semantic/surface.tokens.json, src/tokens/generated/theme.css (if present), src/tokens/generated/tokens.ts (if present), src/tokens/generated/tokens.json (if present), src/tokens/index.ts (if present)</read_first>
  <files>tokens/style-dictionary.config.ts, src/tokens/generated/theme.css, src/tokens/generated/tokens.ts, src/tokens/generated/tokens.json, src/tokens/index.ts</files>
  <behavior>
    - Test 1: The token build emits Tailwind-ready `@theme` variables from the single token source.
    - Test 2: The token build emits docs-friendly JSON and typed TypeScript exports from that same source.
    - Test 3: Consumers import from `src/tokens/index.ts` instead of deep-linking the generated files directly.
  </behavior>
  <action>Create `tokens/style-dictionary.config.ts` that reads from `tokens/foundation/**/*.json` and `tokens/semantic/**/*.json`, then writes three targets: `src/tokens/generated/theme.css`, `src/tokens/generated/tokens.json`, and `src/tokens/generated/tokens.ts`. The CSS output must use Tailwind v4-friendly `@theme` variable declarations such as `--color-*`, `--font-*`, `--shadow-*`, `--radius-*`, and `--breakpoint-*`. The JSON output must preserve enough metadata for Storybook docs consumption, including the Costra traceability extensions. The TypeScript output must export structured objects for `foundation`, `semantic`, and `meta` or `source` data using ESM-friendly syntax. Add `src/tokens/index.ts` as the stable import surface that re-exports the generated tokens and theme stylesheet path without redefining token data. Run the build and commit the generated artifacts themselves so later phases can consume them without requiring an immediate local rebuild.</action>
  <acceptance_criteria>`tokens/style-dictionary.config.ts` contains the exact output filenames `theme.css`, `tokens.json`, and `tokens.ts`; `src/tokens/generated/theme.css` contains `@theme`, `--color-`, `--font-`, and `--breakpoint-`; `src/tokens/generated/tokens.ts` contains `export const foundation` and `export const semantic`; `src/tokens/generated/tokens.json` contains `foundation` and `semantic`; `src/tokens/index.ts` contains `from './generated/tokens'`.</acceptance_criteria>
  <verify>
    <automated>npm run tokens:build && npx vitest run tests/tokens</automated>
  </verify>
  <done>The repository can regenerate all shared token outputs from one source and expose them through a stable import surface.</done>
</task>

</tasks>

<verification>
Run `npm run tokens:build && npx vitest run tests/tokens` after Task 2. If the build introduces any generated file drift, commit the generated output in this plan instead of leaving it untracked.
</verification>

<success_criteria>
Phase 1 now has a working token source, a small alias layer, and shared CSS/JSON/TS outputs that later phases can consume directly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-foundation/01-02-SUMMARY.md`
</output>
