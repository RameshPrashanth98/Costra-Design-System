---
phase: 01-token-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - tokens/manifest/source-map.json
  - tests/tokens/traceability.test.ts
  - src/tokens/index.ts
  - src/tokens/generated/tokens.json
  - src/tokens/generated/tokens.ts
  - src/tokens/generated/theme.css
  - tokens/README.md
autonomous: true
requirements:
  - TOKN-01
  - TOKN-02
  - TOKN-03
  - TOKN-04
must_haves:
  truths:
    - "The public token names can be traced back to exact HTML variables, selectors, or documented icon rules."
    - "The generated token surface is stable enough for Tailwind, Storybook, and component code to consume without reinterpretation."
    - "Token naming and structure are automatically checked so future edits cannot silently drift away from the source HTML."
  artifacts:
    - path: "tokens/manifest/source-map.json"
      provides: "Public-token-to-source traceability map"
    - path: "tests/tokens/traceability.test.ts"
      provides: "Automated token naming and traceability regression checks"
    - path: "tokens/README.md"
      provides: "Consumer guidance for foundation vs semantic token usage"
  key_links:
    - from: "tests/tokens/traceability.test.ts"
      to: "tokens/manifest/source-map.json"
      via: "source coverage assertions"
      pattern: "source-map"
    - from: "tokens/README.md"
      to: "src/tokens/index.ts"
      via: "documented import path"
      pattern: "src/tokens/index.ts"
    - from: "tokens/manifest/source-map.json"
      to: "8.costra-design-system.html"
      via: "sourceCssVar/sourceSelector references"
      pattern: "--c-|type-sample|icon"
---

<objective>
Verify token naming, structure, and source traceability, then harden the public token consumption contract.

Purpose: Finish Phase 1 with explicit auditability and a stable handoff so later Tailwind, Storybook, and component phases inherit a dependable token base instead of a loosely documented one.
Output: Source traceability manifest, regression test, and consumer guidance for the generated token surface.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Design system with codex/.codex/get-shit-done/workflows/execute-plan.md
@D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Design system with codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-token-foundation/01-CONTEXT.md
@.planning/phases/01-token-foundation/01-RESEARCH.md
@.planning/phases/01-token-foundation/01-VALIDATION.md
@.planning/phases/01-token-foundation/01-02-SUMMARY.md
@8.costra-design-system.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the source-traceability manifest and regression checks</name>
  <read_first>.planning/phases/01-token-foundation/01-02-SUMMARY.md, 8.costra-design-system.html, tokens/manifest/extraction-map.json, tokens/manifest/source-map.json (if present), src/tokens/generated/tokens.json, src/tokens/generated/tokens.ts, tests/tokens/traceability.test.ts (if present), tests/tokens/color.test.ts, tests/tokens/typography.test.ts, tests/tokens/layout.test.ts, tests/tokens/iconography.test.ts</read_first>
  <files>tokens/manifest/source-map.json, tests/tokens/traceability.test.ts, src/tokens/generated/tokens.json, src/tokens/generated/tokens.ts, src/tokens/generated/theme.css</files>
  <behavior>
    - Test 1: Every public semantic token and every renamed foundation token maps back to an original HTML CSS variable, selector, or iconography rule.
    - Test 2: The generated outputs preserve that traceability data so Storybook docs and future audits can consume it.
    - Test 3: The token surface rejects structural drift such as missing metadata or renamed breakpoint keys.
  </behavior>
  <action>Create `tokens/manifest/source-map.json` as the explicit bridge between the public token API and the HTML source. Each entry must record the public token path, the original CSS variable or selector (`sourceCssVar` or `sourceSelector`), the HTML section name, and whether the token is a `foundation` token or a `semantic` alias. Cover the renamed color tokens, typography roles, spacing, elevation, radius, grid primitives, and iconography rules or inventory. Add `tests/tokens/traceability.test.ts` that loads the generated token JSON plus the source-map manifest and asserts that the public token set is traceable, the semantic aliases resolve back to foundation tokens, and key tokens like accent color, display typography, grid breakpoints, and icon stroke rules have metadata. If the generated outputs need minor shape changes to expose the metadata cleanly, update `src/tokens/generated/tokens.json`, `src/tokens/generated/tokens.ts`, and `src/tokens/generated/theme.css` through the existing build pipeline rather than hand-editing inconsistent copies.</action>
  <acceptance_criteria>`tokens/manifest/source-map.json` contains the exact keys `"publicPath"`, `"sourceCssVar"`, `"sourceSelector"`, `"sourceSection"`, and `"layer"`; `tokens/manifest/source-map.json` contains `--c-accent`, `.type-sample-d1`, `--grid-columns`, and `stroke-width`; `tests/tokens/traceability.test.ts` contains `describe('token traceability'`; `src/tokens/generated/tokens.json` still contains `foundation` and `semantic` plus traceability metadata; `src/tokens/generated/theme.css` still contains `@theme` after the traceability adjustments.</acceptance_criteria>
  <verify>
    <automated>npm run tokens:build && npx vitest run tests/tokens/traceability.test.ts tests/tokens</automated>
  </verify>
  <done>The public token surface is mechanically traceable back to the HTML source and protected by automated regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Document and lock the stable token consumption surface for downstream phases</name>
  <read_first>.planning/phases/01-token-foundation/01-CONTEXT.md, .planning/phases/01-token-foundation/01-RESEARCH.md, tokens/manifest/source-map.json, src/tokens/index.ts, tokens/README.md (if present), src/tokens/generated/tokens.ts, src/tokens/generated/theme.css</read_first>
  <files>src/tokens/index.ts, tokens/README.md</files>
  <action>Finalize `src/tokens/index.ts` as the single import entrypoint for downstream consumers. It should export the generated `foundation`, `semantic`, and metadata objects without re-shaping them differently from the generated files. Add `tokens/README.md` that documents the exact directory layout (`tokens/foundation`, `tokens/semantic`, `tokens/manifest`, `src/tokens/generated`), the allowed Phase 1 semantic groups (`text`, `surface`, `border`, `interactive`, and any status aliases you actually created), and the exact consumer expectations for later phases: Tailwind should consume `src/tokens/generated/theme.css`, Storybook or docs should consume `src/tokens/generated/tokens.json`, and app or component code should import from `src/tokens/index.ts`. State explicitly that component-specific tokens are out of scope for Phase 1 per D-03.</action>
  <acceptance_criteria>`src/tokens/index.ts` exports `foundation` and `semantic` from `./generated/tokens`; `tokens/README.md` contains the exact strings `tokens/foundation`, `tokens/semantic`, `src/tokens/generated/theme.css`, `src/tokens/generated/tokens.json`, and `src/tokens/index.ts`; `tokens/README.md` contains `component-specific tokens are out of scope for Phase 1`.</acceptance_criteria>
  <verify>
    <automated>npm run tokens:build && npx vitest run tests/tokens</automated>
  </verify>
  <done>The token system is documented and exported through one stable surface for the next phases to consume without reinterpretation.</done>
</task>

</tasks>

<verification>
Run `npm run tokens:build && npx vitest run tests/tokens` as the final plan gate. Confirm the traceability manifest, generated outputs, and README all describe the same public token surface.
</verification>

<success_criteria>
Phase 1 closes with explicit token traceability, a stable generated export contract, and documentation that constrains later phases to the same token source.
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-foundation/01-03-SUMMARY.md`
</output>
