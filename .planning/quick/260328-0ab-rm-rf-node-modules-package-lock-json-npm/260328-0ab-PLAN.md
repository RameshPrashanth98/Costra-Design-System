# Quick Task 260328-0ab Plan

## Objective
Run a clean npm reinstall for the design system and confirm Storybook still builds successfully.

## Tasks

### 1. Reset install artifacts
- Files: `node_modules/`, `package-lock.json`
- Action: Remove the existing install directory and lockfile requested by the user.
- Verify: `node_modules` and `package-lock.json` are absent before reinstall.
- Done: Old install artifacts are removed without touching unrelated files.

### 2. Reinstall dependencies
- Files: `package.json`, regenerated `package-lock.json`, `node_modules/`
- Action: Run `npm install` from the repo root.
- Verify: Install exits successfully and recreates the lockfile and dependency tree.
- Done: Dependencies are installed from the current manifest.

### 3. Validate Storybook production build
- Files: `storybook-static/` or Storybook build output
- Action: Run `npm run build-storybook`.
- Verify: Build exits successfully and emits the static Storybook output.
- Done: Storybook production build completes from the clean install.
