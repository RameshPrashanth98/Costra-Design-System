# Quick Task 260328-18c Plan

**Description:** Fix Vercel deployment install failure caused by Storybook Vite peer dependency conflict
**Date:** 2026-03-27
**Status:** In progress

## Tasks

1. Inspect the committed dependency graph and identify the exact version mismatch causing Vercel `npm install` to fail.
2. Align `package.json` and `package-lock.json` so `vite`, `@storybook/react-vite`, and `@vitejs/plugin-react` resolve to mutually compatible versions.
3. Verify with a clean local install/build path that the dependency conflict is removed.
